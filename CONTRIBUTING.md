# 参与贡献

感谢帮助期刊雷达变得更好。欢迎报告问题、核验期刊来源、改进阅读体验或提交修复。

## 报告问题

本仓库目前未启用 Issues，通过 [Pull Request](https://github.com/linkingoscar/journal-radar/pulls) 接收修复、复现用例与改进建议。描述问题时请说明：

- 使用在线版还是 Windows 本机增强版，以及浏览器和操作系统。
- 复现步骤、预期结果和实际结果。
- 涉及的公开期刊名称、ISSN、DOI 或 RSS 地址；界面问题可附截图。

请先遮盖截图里的个人笔记、邮箱与敏感内容。不要上传自己的阅读备份、SQLite 数据库、访问令牌或整个浏览器配置。来源暂时不可用时，可以提供“管理期刊与数据源”中的错误摘要。

## 修改代码

1. Fork 仓库，从 `main` 创建分支。
2. 按[开发与部署说明](docs/development.md)准备环境。
3. 围绕一个问题修改，复用已有模式，避免混入无关格式化或依赖升级。
4. 运行相关检查；行为修复应有能够复现问题的测试，界面修改应查看实际桌面与手机页面。纯文档修改无需新增应用测试。
5. 提交 Pull Request，说明问题、改动后的行为和实际验证结果；界面变更附前后截图。

完整维护检查：

```sh
python -B -m pytest radar -q -p no:cacheprovider
ruff format --check radar --no-cache
npm run format:check
npm test
```

不要提交生成的 `site/`、数据库、个人备份、依赖目录或测试临时文件。修改应用静态资源时同步考虑服务工作线程缓存版本；引用组件更新遵循[第三方组件说明](docs/citation-vendor.md)。

## 补充期刊与来源

期刊配置入口是 [`radar/journals.json`](radar/journals.json)。请提供可核验的期刊名称、有效 ISSN、出版商或公开来源链接，并检查是否已有同刊或 ISSN 别名，避免重复收录。

清单归属与期刊评级需注明来源和版本；封面、引用样式及其他第三方资源需保留来源和许可说明。个人关注清单可以先通过本机版的新增期刊功能使用，无需修改公共配置。
